#pragma once
#include <iostream>
#include <nlohmann/json.hpp>

namespace mdl {

    // typedef enum {
        
    // } ModuleType;

    // class IModule {
    //     virtual ~IModule() = default;

    //     ModuleType getType();
    // };

    class Notification;
    class LauncherProfile;
}