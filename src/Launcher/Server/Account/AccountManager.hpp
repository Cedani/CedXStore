#pragma once
#include "MySqlDb.hpp"

namespace lau {
    struct Account {
        std::string pseudo;
        std::string password;
    };

    class AccountManager {
        public:

        private:
    };
}